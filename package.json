{
  "name": "opencode-quota",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "format": "prettier --write --log-level warn .",
    "lint": "biome check . && prettier --check --log-level warn ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.0",
    "@types/bun": "latest",
    "prettier": "3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
